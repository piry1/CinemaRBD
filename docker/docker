cd /home/rado/composetest
docker-compose up -d

$link = mysqli_connect('localhost:3307', 'root', 'master', 'Cinema')
etc/mysql/mysql.conf.d/mysqld.cnf
etc/phpmyadmin/config.inic.php
$cfg['AllowArbitraryServer'] = true;
172.18.0.1
apk update
apk add nano

hasła:
root master  / user test
root slave1  / user test
root slave2  / user test
root slave3  / user test
root slave4  / user test

slaveOne	slave
slaveTwo	slave
slaveThree	slave
slaceFour	slave

